<NODE><INFO>rendered by Everything::XML.pm</INFO>
	<author_user table="node" type="user">root</author_user>
	<code table="htmlcode"># This constructs an HTML list menu using the FormMenu
# package of Everything.  Values of the menu come from
# the specified group node.

use Everything::FormMenu;

my ($group,$name,$selected,$size,$multi) = @_;
my $menu = new Everything::FormMenu;

# If they have not specified a name, use the group name
$name ||= $group;
$name = $$NODE{type}{title} . "_$name";

$menu-&gt;addGroup($group);
$menu-&gt;writeScrollingListHTML($query,$name,$selected,$size,$multi);</code>
	<core table="node">0</core>
	<package table="node">0</package>
	<reputation table="node">0</reputation>
	<title table="node">groupList</title>
	<type_nodetype table="node" type="nodetype">htmlcode</type_nodetype>
</NODE>